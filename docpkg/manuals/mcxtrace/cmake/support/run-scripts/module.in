#%Module 1.0
#
#  @FLAVOR@ module for use with 'environment-modules' package:
# 
proc ModulesHelp { } {
        global version modroot

        puts stderr "\tmodules - loads the modules software & application environment"
        puts stderr "\n\tThis adds $modroot/* to several of the"
        puts stderr "\tenvironment variables."
        puts stderr "\n\tVersion $version\n"
}

module-whatis   "loads the @FLAVOR@ environment"


# Define prefix so PATH and MANPATH can be updated.
setenv        @FLAVOR_UPPER@ @CMAKE_INSTALL_PREFIX@/@FLAVOR@/2.5
setenv        @FLAVOR_UPPER@_TOOLS @CMAKE_INSTALL_PREFIX@/@FLAVOR@/2.5/tools/Perl/
prepend-path  PATH          @CMAKE_INSTALL_PREFIX@/@FLAVOR@/2.5/bin
prepend-path  MANPATH       @CMAKE_INSTALL_PREFIX@/@FLAVOR@/2.5/doc/man

